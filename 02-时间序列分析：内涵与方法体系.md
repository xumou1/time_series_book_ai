# 02 · 时间序列分析：内涵与方法体系

> 本章回答「何为时间序列分析」：它的定义、目标、核心概念工具箱，以及从经典统计到深度学习、基础模型的方法论谱系。

---

## 2.1 定义

**时间序列分析（time series analysis）** 是一组**从有序观测中提取结构、理解生成机制，并据此进行推断与预测**的统计与计算方法的总称。

它处在多学科交汇处：

```
统计学（随机过程、推断） ─┐
计量经济学（经济时序建模） ┼→ 时间序列分析 ←┼ 机器学习（预测模型）
信号处理（滤波、频谱）  ─┘                  └ 控制论（状态空间、滤波）
```

不同学科对它的称谓略有侧重：计量经济学强调**经济机制解释与政策推断**，信号处理强调**滤波与频谱**，机器学习强调**预测精度与泛化**。但共享同一套数据形态与核心难题。

---

## 2.2 分析的四大目标（Chatfield 框架）

英国统计学家 Chris Chatfield 在其经典教材中把时间序列分析的目的归纳为四类，至今仍是准确的总纲：

1. **描述（Description）**：用图表与统计量刻画序列——趋势快慢、季节性、波动大小、周期性。如「气温逐年上升 0.13°C/十年」。
2. **建模（Modeling）**：拟合一个能够解释数据生成机制的模型，回答「数据从何而来」。例如用 AR(2) 解释太阳黑子周期、用 GARCH 解释波动聚集（volatility clustering）。
3. **预测（Forecasting）**：基于历史外推未来，给出点预测与区间/概率预测。这是实践中最主要的目标，也是近十年机器学习的战场。
4. **控制（Control）**：根据当前观测实时调整行为——空调恒温、火箭姿态、库存补货、交易执行。此时分析与决策闭环。

> 补充第 5 类近年凸显的目标：**分类、聚类与异常检测**（识别模式、划分行为、发现故障），在工业与医疗中需求巨大，许多深度学习工作（如 GPT4TS、UniTS）将其与预测统一建模。

---

## 2.3 核心概念工具箱

无论方法如何演进，以下概念构成理解与评估一切时间序列工作的「公共语言」：

### 2.3.1 平稳性（Stationarity）

**弱平稳（弱平稳/协方差平稳）**：均值恒定 `E[x_t] = μ`，自协方差只依赖时间间隔 `Cov(x_t, x_{t+k}) = γ(k)`（不随起点 t 变化）。

**严格平稳**：任意有限维联合分布平移不变，比弱平稳强得多，实践中几乎只用弱平稳。

**白噪声（white noise）**：均值为 0、方差恒定、任意非零滞后自相关为 0 的序列——「纯随机」的基准。任何模型都希望残差近似白噪声。

**为什么重要**：绝大多数统计推断（估计、检验、预测区间）以平稳性为前提；对非平稳序列直接建模会产生**伪回归**（Yule 1926 的 nonsense correlation 即其雏形）。因此经典流程的第一步常常是：检验平稳性（ADF、KPSS 检验），非平稳则做差分/去趋势/变换（log、Box-Cox）。

### 2.3.2 自相关与偏自相关（ACF / PACF）

- **自相关函数 ACF** `ρ(k) = γ(k)/γ(0)`：序列与其滞后 k 的相关系数，刻画「记忆长度」。
- **偏自相关 PACF**：剔除中间滞后的间接影响后，滞后 k 的直接依赖。

ACF/PACF 是经典建模的「指纹」：AR(p) 的 PACF 在 p 阶后截尾、MA(q) 的 ACF 在 q 阶后截尾——Box–Jenkins 方法据此识别模型阶数。

### 2.3.3 频域视角：谱密度

任何平稳序列可看作不同频率正弦/余弦波的叠加，**谱密度（spectral density）** 描述各频率的能量分布（ACF 的傅里叶变换）。周期结构（如太阳黑子的 11 年周期）会在频谱上呈现尖峰。频域方法（周期图、窗谱估计、小波）在信号处理与气候学中尤其重要，Yule 之前的主流正是 Schuster 的周期图法。

### 2.3.4 模型选择与评估

- **信息准则**：AIC / BIC（似然 + 复杂度惩罚），在模型族内选择平衡拟合与简洁的阶数。
- **样本外评估**：时间序列不可随机划分训练/测试（泄漏），必须按**时间顺序**切分（rolling / expanding window），并可配合交叉验证的时序版本（如 TimeSeriesSplit）。
- **常用指标**：MAE、RMSE、MAPE、对称 MAPE、MASE（以朴素基线归一化）、CRPS / Quantile Loss（概率预测）、覆盖率（区间预测）。

---

## 2.4 方法论谱系

时间序列分析的方法演进可以概括为「统计 → 机器学习 → 深度学习 → 基础模型」四个世代（详细编年见第 03 章，此处给体系）。

### 第一世代 · 经典统计与计量（~1900s–1980s）

| 方法 | 代表 | 适用 |
|---|---|---|
| 指数平滑族 | Holt–Winters（趋势+季节） | 短期预测，稳健简单 |
| ARIMA / SARIMA / ARIMAX | Box & Jenkins (1970) | 单变量平稳化后的线性预测 |
| 状态空间 + 卡尔曼滤波 | Kalman (1960) | 含噪声系统的实时滤波与估计，可含外生变量与缺失值 |
| ARCH / GARCH | Engle (1982) / Bollerslev (1986) | 波动率建模（金融） |
| VAR / 协整 / 误差修正 | Engle & Granger (1987) | 多变量联动与长期均衡 |
| 马尔可夫机制转换 | Hamilton (1989) | 经济周期、体制切换 |
| 谱分析 / 小波 | Tukey、Daubechies | 周期探测、去噪、多尺度 |

**优点**：可解释、参数少、小样本可靠、有推断理论（置信区间）。**局限**：线性假设强，难以捕捉高维非线性与长程复杂依赖。

### 第二世代 · 机器学习（~1990s–2010s）

- 支持向量回归（SVR）、随机森林（Breiman 2001）、梯度提升树（GBDT / XGBoost / LightGBM）、kNN 等。
- 做法：把滞后窗口组织成特征矩阵 `(x_{t-1}, x_{t-2}, …, x_{t-p}) → x_t`，本质是**非线性自回归**。
- **优点**：可处理高维外生特征与非线性。**局限**：特征工程依赖强、不原生处理顺序结构、长期预测需递归/多步策略。

### 第三世代 · 深度学习（~2010s–2020s）

- 序列模型：RNN / LSTM / GRU（Hochreiter & Schmidhuber 1997 提出、2010s 复兴）。
- 卷积：WaveNet（2016）、TCN（2018）——因果膨胀卷积，并行性好。
- Transformer：Informer（2021，长序列高效注意力）、Autoformer（2021，分解+自相关）、PatchTST（2023，分片+通道独立）、iTransformer（2024，倒置注意力）。
- 纯 MLP 结构：N-BEATS（2019）、N-HiTS、TiDE——提醒人们结构并非越复杂越好。
- 状态空间：Mamba / S-Mamba（2024）——近线性复杂度的长程建模新路线。
- 生成式：TimeGAN（2019）、TimeGrad（2021）、CSDI（2021）、TimeDiff（2023）——扩散/对抗式概率生成。

**优点**：端到端、自动特征学习、擅长高维非线性与大样本。**局限**：数据饥渴、可解释性差、在样本外与统计基线竞争时优势并不总是成立（见 2.5 的线性基线之争）。

### 第四世代 · 基础模型（~2022–）

- 在大规模跨域语料上预训练，零样本/少样本泛化到新数据集：TimeGPT（2023）、TimesFM（2023/24）、Chronos（2024）、Moirai（2024）、Time-MoE（2024）、UniTS（2024）、MOMENT（2024）等。
- 大语言模型（LLM）介入：GPT4TS、Time-LLM、LLMTime（2023）。
- 详见第 04 章。

---

## 2.5 一个贯穿始终的方法论张力：线性基线之争

2023 年，论文 *"Are Transformers Effective for Time Series Forecasting?"*（LTSF-Linear，AAAI 2023）给出了当时最有冲击力的结果：**一个单层线性模型在 9 个真实数据集上全面超越当时复杂的 Transformer 预测模型**。随后 N-BEATS/TiDE 等纯 MLP 也表现强劲。

这一结果把领域内的争论推到台前：

- **统计与线性的底线很高**：ETS、ARIMA、简单线性回归在大量基准上依然是极难战胜的对手；M4/M5 预测竞赛的冠军也多为统计+机器学习**集成**方案，而非纯深度模型。
- **复杂模型必须证明自己的增量价值**：基准（ETT、Weather、Exchange 等）日渐饱和，新方法以线性/统计基线为最低门槛已成为审稿共识。

这一张力恰恰是时间序列分析的独特之处——**序列结构的简单性让简单模型受益，但复杂依赖又需要复杂模型**，没有放之四海而皆准的赢家。

---

## 2.6 分析的一般工作流

无论使用哪一代方法，严谨的流程大体一致（Box–Jenkins 三阶段是经典模板，数据驱动范式是对它的扩展）：

1. **理解数据**：可视化（时序图、自相关图、季节性分解）、检查缺失/异常、明确采样与预测需求。
2. **平稳化与预处理**：差分、变换、去趋势、归一化（深度学习时代多用 RevIN 等可逆实例归一化）。
3. **模型识别与选择**：根据 ACF/PACF、信息准则（经典），或架构选择/超参搜索（深度学习）。
4. **估计/训练**：极大似然（经典）、梯度下降（深度）、滚动窗口训练（考虑分布漂移）。
5. **诊断**：残差是否白噪声（Ljung–Box 检验）、是否还存在结构；深度学习则以验证集指标为准。
6. **预测与评估**：样本外评估（按时间切分）、不确定性量化（区间/分位数/CRPS）、与简单基线对比。

---

## 2.7 挑战与开放问题（详版见第 04 章）

- **非平稳与概念漂移**：真实分布持续变化，训练-测试分布错位是根本难题。
- **不确定性量化**：多数深度模型输出点估计，概率预测（分位数、轨迹联合分布）仍在完善。
- **可解释性与因果**：黑箱模型难以为金融、医疗等高风险决策提供依据；时序归因方法（如 SHAP 的时序应用）缺乏理论保证。
- **长序列与高维**：超长外推、多变量高维依赖仍具挑战。
- **评估协议混乱**：泄漏与预处理不一致导致结果难复现，数据中心的再评测成为重要支线。

---

## 2.8 小结

- 时间序列分析 = 描述 + 建模 + 预测 + 控制（+ 分类/异常检测）。
- 公共语言：平稳性、白噪声、ACF/PACF、谱密度、样本外评估。
- 方法论四世代：经典统计 → 机器学习 → 深度学习 → 基础模型，各有优势与边界。
- 领域独特张力：**简单线性/统计基线极强**，新方法必须证明增量价值。

接下来第 03 章按时间线展开这四世代的历史脉络。
