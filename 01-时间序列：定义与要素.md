# 01 · 时间序列：定义与要素

> 本章回答「何为时间序列」：它的严格定义、区别于其他数据类型的内在特性、分类体系、结构成分，以及真实世界中的典型实例。

---

## 1.1 直观理解与定义

**时间序列（time series）** 是指同一对象（变量）在**时间顺序上依次排列**的观测值集合。小到一次心搏的电压波形，大到数十年的全球气温记录、逐秒的股票成交价、每一小时的电网负荷，都是时间序列。

形式化地，一个时间序列是一组按时间索引的观测值：

```
{ x(t) : t ∈ T }
```

其中 `T` 是时间索引集（离散或连续），`x(t)` 是在时刻 `t` 的观测值。实际中最常见的是**等间隔离散时间序列**，记作：

```
x₁, x₂, …, xₜ, …, x_T
```

下标 `t` 为整数（如第 1 秒、第 1 小时、第 1 月）。

### 一个容易混淆的点

- **时间序列（time series）**：同一个变量随时间的变化，例如「北京逐日最高气温」。
- **横截面数据（cross-sectional data）**：同一时刻多个对象，例如「2024 年 12 月 31 日全国各省气温」。
- **面板数据（panel / longitudinal data）**：多个对象×多个时刻，例如「各省×逐日气温」，本质是多个时间序列的并列，又称纵向数据。

三者的区分是数据分析的起点：时间序列研究**随时间演化的规律**，而横截面研究**个体间的差异**。

---

## 1.2 时间序列的三个内在特征

时间序列之所以需要专门的学科与专门的方法，是因为它与「可随机打乱顺序的普通样本」有本质区别：

1. **时间的有向性（不可逆性）**：时间只能单向流动。今天的气温可以影响明天的，反过来不成立。因此**因果与依赖具有方向**，预测只能向后、不能向前（除非重建历史）。样本之间不能随机重排——打乱顺序会彻底摧毁序列结构。

2. **相邻观测的相关性（序列相关）**：普通统计假设样本独立同分布（i.i.d.）；而时间序列中相邻观测通常高度相关（今天的温度接近昨天）。这种**自相关（autocorrelation）**既是结构信息的来源，也破坏了传统 i.i.d. 假设，需要专门处理。

3. **非平稳性（non-stationarity）是常态而非例外**：均值会漂移（经济趋势）、波动会变化（金融危机前后）、季节会切换（销售淡旺季）。真实序列几乎都不是平稳的，平稳性是模型化时的一种简化假设或需要显式处理的目标。

---

## 1.3 分类体系（类型学）

从不同维度，时间序列可以划分为：

| 维度 | 类别 | 说明与例子 |
|---|---|---|
| 时间索引 | 离散 / 连续 | 离散：按日、月采样；连续：模拟信号、神经脉冲（通常仍需离散化处理） |
| 采样间隔 | 等间隔 / 不等间隔 | 等间隔：小时级传感器；不等间隔：门诊就诊记录、天文观测（需要专门的连续时间模型） |
| 变量个数 | 单变量 / 多变量 | 单变量：一个变量自身的历史；多变量：同一组时间点的多个相关变量（如温度+湿度+风速），变量间存在互相关 |
| 观测完整性 | 完整 / 缺失 | 缺失可能随机（传感器偶发故障）或系统性（按周缺失），影响方法选择 |
| 生成机制 | 确定性 / 随机性 | 严格确定：钟摆、程序输出；实际几乎都含随机成分，故用随机过程建模 |
| 平稳性 | 平稳 / 非平稳 | 见 1.4 与第 02 章 |
| 空间属性 | 单地点 / 空间时序（spatio-temporal） | 气象站网络、交通路网：既随时间又随空间变化，属更复杂的拓展 |

---

## 1.4 结构成分：趋势、季节、循环与噪声

传统时间序列分析的核心工作之一，是把序列**分解**为若干可解释的成分。经典加法模型写作：

```
x(t) = T(t) + S(t) + C(t) + ε(t)
```

（乘法模型 `x(t) = T(t) × S(t) × C(t) × ε(t)` 常用于振幅随水平变化的序列，如零售额。）

| 成分 | 含义 | 例子 |
|---|---|---|
| **趋势 T(t)**（trend） | 长期缓慢的方向性变化 | GDP 长期增长、全球变暖 |
| **季节性 S(t)**（seasonality） | 固定周期（周期已知且不变）的波动 | 每年 12 月购物高峰、每日通勤双峰 |
| **循环 C(t)**（cyclicality） | 周期不固定或很长的起伏 | 经济景气循环（数年至数十年） |
| **残差/噪声 ε(t)**（residual/noise） | 剔除上述成分后的随机波动 | 无法解释的观测误差与随机冲击 |

> 注意区分「季节」与「循环」：季节的周期**已知且固定**（24 小时、一周 7 天、一年 12 月），循环的周期**未知且可变**（如 5~8 年的经济周期）。在英文文献中，seasonal vs cyclical 的区别即在于此。

分解思想贯穿始终：经典统计中的 STL 分解、X-13ARIMA-SEAT；深度学习中的 Autoformer 将「分解」内嵌为网络模块；基础模型时代依然以「趋势+季节」为可解释结构。

---

## 1.5 现实世界中的时间序列

时间序列无处不在，是几乎所有定量学科的共同数据形态：

| 领域 | 典型序列 | 采样尺度 |
|---|---|---|
| 宏观经济 | GDP、CPI、失业率、利率 | 月/季/年 |
| 金融 | 股价、收益率、波动率、成交量 | 秒~日 |
| 气候与气象 | 气温、降水、海平面、厄尔尼诺指数 | 小时~年 |
| 能源 | 电力负荷、风电出力、天然气价 | 分钟~小时 |
| 工业与物联网 | 振动、温度、压力、设备状态 | 毫秒~分钟 |
| 医疗健康 | 心电图 ECG、脑电 EEG、血氧、发病率 | 毫秒~天 |
| 交通 | 车流量、客流、航班延误、物流量 | 分钟~小时 |
| 零售与电商 | 日销售额、SKU 需求、客流 | 天 |
| 语音音频 | 声波采样 | 8k~48kHz |
| 天文与地球物理 | 太阳黑子数、地震波形、地磁 | 秒~年 |
| 生物 | 种群数量、基因表达、心率变异性 | 分钟~年 |
| 网络与安全 | 网络流量、入侵告警、服务器负载 | 毫秒~秒 |

**太阳黑子**是时间序列分析的「果蝇」——G. U. Yule 1927 年的开山之作正是用自回归模型分析 Wolfer 太阳黑子数（见第 03 章），此后一直作为标准测试数据沿用至今。

---

## 1.6 数据特征带来的现实挑战

1. **缺失值**：传感器断线、假期停更。处理方式：插补（线性/样条/模型插补）、或选择容忍缺失的方法。
2. **不等间隔采样**：天文、医学随访数据。处理方式：重采样对齐，或使用连续时间模型（卡尔曼滤波、神经 ODE、Gaussian Process）。
3. **多尺度与异质性**：同一系统内不同变量的采样率不同（如高频交易 vs 日线宏观），跨频率对齐是基础模型要解决的关键问题之一（Moirai 的「跨频率训练」）。
4. **异常与离群点**：突发冲击（疫情、灾害）会扭曲模型，既可能是噪声也可能是重要信号。
5. **数据漂移**：生成数据的机制本身会随时间变化（如消费习惯改变），「训练时分布 ≠ 测试时分布」是长期难题。

---

## 1.7 小结

- 时间序列 = 按时间顺序排列的观测，核心特征是**方向性、序列相关、非平稳**。
- 它区别于横截面与面板数据，需要专门的方法论。
- 经典结构 = 趋势 + 季节 + 循环 + 噪声，分解是贯穿百年历史的核心思想。
- 它是统计学、计量经济学、信号处理、控制论与机器学习的共同研究对象。

下一章回答第二个问题：**对这样一串数据，「分析」到底意味着什么、能做什么、用什么工具箱。**
